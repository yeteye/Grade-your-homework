# Datasets

`lcqmc/` 保留原项目的三份中文文本匹配数据。实际格式均为制表符分隔的三列：`text_a`、`text_b`、`label`，无表头，标签为 0 / 1。

- `train.tsv`：原训练集。
- `dev.tsv`：原 `dev.csv`，仅修正扩展名。
- `test.tsv`：原 `test.csv`，仅修正扩展名。

数据内容未改。Web 启动和单份推理不依赖这些数据文件；仅训练和离线评估工具读取它们。仓库原有材料未提供完整的数据来源与授权记录，后续发布数据或撰写研究报告时应补充溯源说明。
