# 成员 A 关键 AI 协作记录

记录方式：关键对话摘要和执行决策，不冒充逐字聊天导出。原始对话保留在本 Codex 任务中。

## 已确认的用户约束

1. 软件测试模块一基于 `2da8824`，不在报告涉及其之前的修改。
2. 本次完成成员 A 的评分和输入校验工作；成员 B 由队友独立完成。
3. 每做完一个实质工作单元提交一次，不一次性提交全部成果，不代队友提交。
4. 寻找缺陷需要细致，必须用复现和回归证据支撑。

## 开始阶段

- 已核对当前基线为 `2da8824`，创建 `codex/m1-a-scoring` 工作分支。
- 保留仓库原有 Git 提交身份，不替换为队友身份。
- 基线已有开发检查，课程用例需单独编号、明确设计依据和来源。
- 本次会检查极端数值、规范化一致性、评分权重组合及异常不落库。
- AI 辅助生成脚本和文档，执行结果由实际运行产生。成员 A 在课程提交和现场展示前需复跑、理解并确认本人交付内容。

## 2026-09-15 测试发现

- AI 提议检查数值溢出、规范化去重、模型分数进入权重公式前的有效性、云端响应的异常回退。
- 实际验证：使用冻结基线 `2da8824` 的 Git archive 运行 22 条 A 用例；既有 18 条通过，新增加的 A-019～A-022 共 4 条失败。
- 对应缺陷：A-DEF-001～A-DEF-004。每项均有明确输入、预期、实际结果和版本化的机器可读执行记录。
- 复现中的本地模型和云端响应由测试替身提供；这验证异常输入处理，不代表真实模型的准确率。
